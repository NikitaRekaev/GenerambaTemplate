// swift-tools-version: 5.5
// The swift-tools-version declares the minimum version of Swift required to build this package.

import PackageDescription

let package = Package(
    name: "{{ module_info.name }}",
    platforms: [
        .iOS(.v13)
    ],
    products: [
        .library(
            name: "{{ module_info.name }}",
            targets: ["{{ module_info.name }}"])
    ],
    dependencies: [
        .package(path: "../../../Core"),
        .package(path: "../../../DesignCore"),
        .package(path: "../../../Mediator"),
        .package(url: "https://github.com/SnapKit/SnapKit.git", from: "5.6.0")
    ],
    targets: [
        .target(
            name: "{{ module_info.name }}",
            dependencies: ["Core", "DesignCore", "Mediator", "SnapKit"],
            path: "Sources"
        )
    ],
    swiftLanguageVersions: [SwiftVersion.version("5.5")]
)
